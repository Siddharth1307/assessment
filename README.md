# ✨ Frontend

To start the project in dev mode:

```
cd frontend
npm i         # if not done before
npm start
```

To build the project:

```
cd frontend
npm i         # if not done before
npm run build
```
